# 学习通网页课件接入说明

## 先说结论

学习通预览 PPT 时会把幻灯片转换成静态页面，不执行 PPT 里的 HTML、JavaScript、OLE 对象或附件。因此，小游戏不能通过“上传 PPT”留在幻灯片内部运行。

要让学生在课程章节页面直接操作，需要把本文件夹部署成 HTTPS 网页，再由学习通章节编辑器使用“网页、外部网页、HTML5 课件或第三方资源”一类组件嵌入。各学校的组件名称和权限可能不同。

## 文件用途

- `index.html`：总复习入口，与 `20-方药智慧训练场.html` 内容一致。
- `01` 至 `19`：各章节小游戏。
- `iframe-test.html`：在普通浏览器中模拟被学习通 iframe 内嵌的效果。
- `manifest.json`：文件清单和校验值。
- `_headers`：在支持该配置的静态托管服务中允许超星页面嵌入。

## 推荐接入步骤

1. 将本文件夹完整上传到学校网站、课程资源服务器或其他支持 HTTPS 的静态网站空间。
2. 确认 `https://你的域名/路径/index.html` 在电脑和手机浏览器中都能打开并答题。
3. 在学习通电脑端进入课程的章节编辑页面，查找“网页、外部网页、HTML5 课件、第三方资源”一类入口。
4. 填入网页地址，并选择“内嵌显示、页面内打开”一类选项。不要继续把网页作为 PPT 附件插入。
5. 用学生账号分别在电脑端和学习通 App 中试做一局，检查点击、滚动、计分和刷新后的记录。

## 结合当前章节编辑界面的测试方法

当前截图的右侧只有“链接”，没有单独的“网页/HTML5 课件”组件。工具栏中的 `</>` 图标可能是 HTML 源代码入口，也可能只是代码块工具。

1. 点击工具栏中的 `</>`。
2. 如果弹窗标题类似“源代码、HTML、代码视图”，先粘贴 `学习通iframe测试代码.txt` 中的测试代码。
3. 保存并预览。如果按钮能点击并改变文字，说明该入口允许交互式 iframe。
4. 如果代码原样显示为文字、iframe 被删除或保存后只剩空白，说明平台进行了安全过滤，教师端不能直接内嵌，需要学校管理员或超星服务人员开放网页课件组件。

## 如果只能显示链接

这说明学校当前的超星后台没有开放网页内嵌组件，或服务器域名没有加入允许名单。教师端无法靠修改 PPT 绕过。请把本 ZIP 和部署后的 HTTPS 地址交给学校平台管理员或超星服务人员，请他们开放“外部网页/HTML5 课件内嵌”，并将网页域名加入白名单。

可以直接这样描述需求：

> 课程章节需要内嵌一个 HTTPS 的 HTML5 互动课件，要求在网页端和学习通 App 内直接操作，不跳出到系统浏览器。请确认本校泛雅平台是否已开放外部网页或 HTML5 课件组件，并为课件域名配置 iframe 白名单。

## 服务器要求

- 必须使用 HTTPS。
- 不得返回 `X-Frame-Options: DENY` 或 `X-Frame-Options: SAMEORIGIN`。
- `Content-Security-Policy` 的 `frame-ancestors` 需要允许学校的超星域名。
- MIME 类型应为 `text/html; charset=utf-8`。
- 不要给 HTML 文件设置强制下载响应头。

## 成绩说明

当前分数、最高分和错题记录保存在学生当前设备的网页存储中，不会自动进入学习通成绩中心。若要把分数回传学习通，需要学校或超星提供开放接口、LTI 或专用 HTML5 课件接口，这属于另一项系统对接工作。
